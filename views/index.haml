%a{:href => '/'}
  %img{:id => 'weeks-small', :src => '/3_weeks_small.png', :alt => '3 weeks in Brazil'}
  
- @grouped.each do |location, posts|
  - posts.each do |post|
    - if @all_grouped[location].first == post
      %h1.index= location
    %p.post= post.title
#navigation
  = next_page
  = previous_page